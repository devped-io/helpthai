export enum ProjectSide {
  helper = 'helper',
  helpee = 'helpee',
}

export enum ProjectStatus {
  open = 'open',
  close = 'close',
}

export enum ProjectRole {
  owner = 'owner',
  staff = 'staff'
}
